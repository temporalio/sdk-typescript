{
  "name": "typescript-sdk",
  "private": true,
  "keywords": [
    "temporal",
    "workflow",
    "isolate"
  ],
  "homepage": "https://github.com/temporalio/sdk-typescript#readme",
  "bugs": {
    "url": "https://github.com/temporalio/sdk-typescript/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/temporalio/sdk-typescript.git"
  },
  "license": "MIT",
  "author": "Temporal Technologies Inc. <sdk@temporal.io>",
  "directories": {
    "doc": "docs"
  },
  "files": [],
  "scripts": {
    "rebuild": "pnpm run clean && pnpm run build",
    "build": "pnpm --recursive --stream run build",
    "build:watch": "pnpm run build:protos && tsc --build --watch packages/*/tsconfig.json",
    "build:protos": "tsx ./packages/proto/scripts/compile-proto.ts",
    "test": "pnpm --recursive --parallel --workspace-concurrency=1 --stream run test",
    "test:watch": "pnpm --recursive --stream run test:watch",
    "ci-stress": "node ./packages/test/lib/load/run-all-stress-ci-scenarios.js",
    "ci-nightly": "node ./packages/test/lib/load/run-all-nightly-scenarios.js",
    "lint": "oxlint --fix --no-error-on-unmatched-pattern packages/*/src contrib/*/src && prettier --write . && pnpm --recursive --stream run lint",
    "lint:check": "oxlint --no-error-on-unmatched-pattern packages/*/src contrib/*/src && prettier --end-of-line auto --check . && pnpm --recursive --stream run lint:check",
    "lint:prune": "ts-prune --error -p tsconfig.prune.json --ignore \"used in module\" --skip \".d.ts\"",
    "commitlint": "commitlint --from origin/main",
    "format": "prettier --write . && pnpm --recursive --stream run format",
    "clean": "tsx ./scripts/clean.ts",
    "docs": "cd packages/docs && pnpm run maybe-install-deps-and-build-docs",
    "ava": "pnpm -C packages/test exec ava"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.0.0",
    "@commitlint/config-conventional": "^20.0.0",
    "@tsconfig/node20": "^20.1.4",
    "@typescript/native-preview": "7.0.0-dev.20260421.2",
    "@types/fs-extra": "^11.0.4",
    "@types/node": "^20.19.39",
    "@typescript-eslint/parser": "^8.59.3",
    "arg": "^5.0.2",
    "eslint": "^9.26.0",
    "eslint-import-resolver-typescript": "^3.6.1",
    "eslint-plugin-import": "^2.31.0",
    "fs-extra": "^11.2.0",
    "json5": "^2.2.3",
    "oxlint": "^1.64.0",
    "oxlint-tsgolint": "^0.22.1",
    "prettier": "^3.1.1",
    "ts-prune": "^0.10.3",
    "tsx": "^4.20.6",
    "typescript": "^6.0.3",
    "verdaccio": "^6.5.0"
  },
  "engines": {
    "node": ">= 20.0.0",
    "pnpm": ">= 10.27.0",
    "rustc": ">= 1.53.0"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "10.27.0",
      "onFail": "warn"
    }
  },
  "pnpm": {
    "patchedDependencies": {
      "protobufjs@7.5.8": "patches/protobufjs@7.5.8.patch"
    }
  }
}
