// Empty module used by the StrandsPlugin's bundler config to replace
// transport modules and `node:*` schemes referenced by
// `@strands-agents/sdk/mcp-config.js` that are unreachable in workflow code.
export {};
